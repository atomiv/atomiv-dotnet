version: 1.0.{build}
image: Visual Studio 2017

services:
  - mssql2017

install:
  - choco install googlechrome --ignore-checksums

before_build:
- ps: dotnet restore
- ps: nuget restore
- ps: Get-Location
- ps: .\configure.ps1
- ps: Get-Location

build:
  verbosity: minimal
  project: Optivem.Framework.sln